import { Button, Container } from '@mui/material'
import React from 'react'

const Admin = () => {
  return (
    <Container>
      <Button variant={'outline'}>Создать заявку</Button>
      <Button variant={'outline'}>Создать объект</Button>

    </Container>
  )
}

export default Admin
