import {makeStyles} from '@mui/styles'

const popoverStyles = makeStyles({
  container: {
    marinLeft: 15,
    marginRight: 15,
    marginBottom: 15,
    marginTop: 15,
    maxWidth: 600,
    maxHeight: 800
  },
  listItem: {
    marginTop: 20,
    marginBottom: 20 
  },
})

export default popoverStyles;